<h1>Create a new tenant</h1>

<%= form_with(model: @tenant, local: true) do |form| %>
  <%= react_component("individual/ProfileForm", props={ id: @tenant.id, mode: @mode, type: @type, fieldNames: @field_names, fieldTypes: @field_types, niceFieldNames: @nice_field_names, prevValues: @prev_values, current_userID: @current_userID }) %>
<% end %>