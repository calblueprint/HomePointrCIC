<%= form_with(model: @tenant, local: true) do |form| %>
  <%= react_component("RATenantView", props={ id: @tenant.id, mode: @mode, name: @name, description: @description, tagValues: @tag_values, applications: @applications, status: @status }) %>
<% end %>
