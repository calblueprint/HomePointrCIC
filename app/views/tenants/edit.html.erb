<h1>Tenants#edit</h1>
<p>Find me in app/views/tenants/edit.html.erb</p>


<%= form_with(model: @tenant, local: true) do |form| %>
  <%= react_component("ProfileForm", props={ mode: @mode, type: @type, fieldNames: @field_names, fieldTypes: @field_types, prevValues: @prev_values }) %>
<% end %>

