<%= form_with(model: @referral_agency, local: true) do |form| %>
  <%= react_component("individual/ProfileForm", props={ id: @referral_agency.id, mode: @mode, type: @type, fieldNames: @field_names, fieldTypes: @field_types, niceFieldNames: @nice_field_names, prevValues: @prev_values }) %>
<% end %>