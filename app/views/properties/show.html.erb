<%= form_with(model: @property, local: true) do |form| %>
  <%= react_component("PropertyView", props={ id: @property.id, name: @name, description: @description, tagValues: @tag_values }) %>
<% end %>