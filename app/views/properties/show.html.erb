<%= form_with(model: @property, local: true) do |form| %>
  <%= react_component("PropertyShowPage", props={ id: @property.id, name: @name, description: @description, tagValues: @tag_values, images: @images }) %>
<% end %>

