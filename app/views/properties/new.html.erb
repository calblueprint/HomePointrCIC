<h1>Create New Property</h1>

<%= form_with(model: @property, local: true) do |form| %>
  <%= react_component("ProfileForm", props={ id: @property.id, mode: @mode, type: @type, fieldNames: @field_names, fieldTypes: @field_types, prevValues: @prev_values }) %>
<% end %>
