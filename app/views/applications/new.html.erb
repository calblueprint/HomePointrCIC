<%= form_with(model: @application, local: true) do |form| %>
  <%= react_component("ApplicationsPairing", props={ tenants: @tenants, properties: @properties}) %>
  <%= form.file_field :form%>
<% end %>
